# Affine

## How to install with docker

[Self-host AFFiNE](https://docs.affine.pro/self-host-affine/install/docker-compose-recommend)


## Issues with AFFiNE
### Bad worksapce type names
During creating workspace
- `local storage` means that it's stored in the browser
- `AFFiNE Selfhosted Cloud` means that it store locally e.g. ./storage/blobs
